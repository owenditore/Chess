<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessUI"
        mc:Ignorable="d"
        Title="Chess" Width="600" SizeToContent="Height"
        Icon="/Assets/icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#101010">
    <Viewbox>
        <!--<Grid x:Name="MainMenu">
            <Button x:Name="StartGame"
                    Height="75"
                    Width="300"
                    Click="OnClick1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                
            </Button>
        </Grid>-->
              
        <Grid x:Name="BoardGrid"
              Width="600"
              Height="600"
              MouseDown="BoardGrid_MouseDown"
              Visibility="Visible">

            <Grid.Background>
                <ImageBrush ImageSource="/Assets/Board.png"/>
            </Grid.Background>

            <Button x:Name="StartGame"
                    Height="75"
                    Width="300"
                    Click="Button_Click"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Content="Start Game"
                    FontSize="30"
                    Visibility="Visible">
            </Button>

            <UniformGrid x:Name="HighlightGrid"
                         Rows="8"
                         Columns="8">
            </UniformGrid>

            <UniformGrid x:Name="PieceGrid"
                         Rows="8"
                         Columns="8">

                <!--<UniformGrid.LayoutTransform>
                    <RotateTransform Angle="180"/>
                </UniformGrid.LayoutTransform>

                <UniformGrid.Resources>
                    --><!-- 2. Rotate children back 180 degrees so they stay upright --><!--
                    <Style TargetType="FrameworkElement">
                        <Setter Property="LayoutTransform">
                            <Setter.Value>
                                <RotateTransform Angle="180"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </UniformGrid.Resources>-->

            </UniformGrid>

            <StackPanel Grid.Row="300" Grid.Column="300">
                <TextBlock x:Name="Checkmate" Text="Checkmate!" FontSize="30" Visibility="Hidden"/>
                <TextBlock x:Name="Draw" Text="Draw" FontSize="30" Visibility="Hidden"/>
            </StackPanel>

        </Grid>

    </Viewbox>
    

</Window>
